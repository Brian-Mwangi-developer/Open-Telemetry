import type { NextConfig } from "next";


const nextConfig: NextConfig = {
  //Telemetry using experimental Initialized by default  calling the instrumentation.ts file

};

export default nextConfig;
